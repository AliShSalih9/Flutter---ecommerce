// List of Enums
/// the cannot be created inside a class
library;

enum productType { Single, Variable }

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethod { paypal, googlePay, visa, masterCard, creidtCard, fib }
